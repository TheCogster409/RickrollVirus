#include <iostream>
#include <fstream>
#include <windows.h>
#include <S:/Coding/LanguageInstalls/curl/include/curl/curl.h>
#include <string>
#include <thread>
#include <chrono>

int WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nShowCmd){
    return 0;
}